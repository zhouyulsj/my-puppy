在已有的 index.html 基础上，实现"健康评估"Tab的完整功能。只修改和追加必要部分。

【功能流程】
1. 用户点击"生成健康管家方案"后，视图切换到方案页
2. 默认显示"健康评估"Tab
3. 显示loading动画（旋转的爪印图标 + "AI正在分析毛球的健康状况..."）
4. 调用 callAI(buildHealthAssessmentPrompt(currentPet, breedInfo))
5. 成功：渲染结果；失败：使用fallback数据；都失败：显示"生成失败，请重试"按钮

【渲染设计】

风险提示区域：
- 每条风险显示为卡片，左侧竖条颜色标识等级：
  - high → 红色 #EF4444
  - medium → 黄色 #F59E0B  
  - low → 绿色 #10B981
- 卡片内容：风险标题（加粗）+ 描述 + 建议措施（灰色小字）
- 按等级从高到低排序

日常养护建议区域：
- 四个方向（饮食/运动/环境/心理）用2x2网格布局
- 每个方向：图标 + 标题 + 建议列表（每条前加•）
- 图标：饮食🍽️ 运动🏃 环境🏠 心理❤️

特别注意事项：
- 底部一条横幅样式，浅蓝背景，显示age_notes列表

【样式要求】
- 卡片间距16px，内边距20px
- 风险卡片有左侧4px圆角竖条
- 整体有淡入上滑动画
- 手机端2x2网格变为单列

请输出需要修改和追加的完整代码（标明是修改还是追加，以及具体位置）。